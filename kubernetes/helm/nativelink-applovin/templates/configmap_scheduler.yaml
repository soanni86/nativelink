apiVersion: v1
kind: ConfigMap
metadata:
  name: nativelink-config-scheduler
  labels:
    {{- include "nativelink-applovin.labels" . | nindent 4 }}
    component: scheduler
data:
  conf.json5: |-
    {{ .Values.scheduler.conf | nindent 4 }}

